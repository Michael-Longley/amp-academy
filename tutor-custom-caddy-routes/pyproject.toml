[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tutor-custom-caddy-routes"
version = "0.1.0"
dependencies = ["tutor>=15.0.0", "PyYAML>=6.0"]

[project.entry-points."tutor.plugin.v1"]
custom-caddy-routes = "tutorcustomcaddyroutes"
